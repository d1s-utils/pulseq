spring:
  profiles:
    active: production

  application:
    name: pulseq

  data:
    mongodb:
      database: pulseq-data
      auto-index-creation: on
      host: localhost
      port: 27017

management:
  endpoints:
    web:
      exposure:
        include: "*"

server:
  error:
    whitelabel:
      enabled: false

pulseq:
  version: 0.1.2-alpha.3
  repository: https://github.com/d1snin/pulseq

  security:
    secured-paths: /api/beat/
    secret: change-this

  view:
    enabled: true