pulseq:
  activity:
    activity-delimiter: "10s"
    common: "20s"

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: "*"

logging:
  level:
    org.springframework: debug